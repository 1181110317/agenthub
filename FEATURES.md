# AgentHub 功能路线图（候选清单）

> 状态：仅规划，未实现。挑选编号告诉开发者即可开工。
> 最后更新：2026-09-08

---

## 🌟 第一梯队：高价值 · 低难度（建议优先）

| # | 功能 | 说明 | 预估 |
|---|------|------|------|
| 1 | 命令面板 | Ctrl+Shift+P 全局命令入口（切 Agent/开终端/导出/换主题…），ZCode 同款 | 小 |
| 2 | 预设短语库 | 常用指令（"继续""审查刚才的改动""写测试"）一键插入，支持自定义管理 | 小 |
| 3 | 系统提示词 | 会话级自定义 system prompt（`--append-system-prompt`），按项目记忆默认值 | 小 |
| 4 | 会话归档 | 不想删又碍眼的会话一键归档收起，需要时恢复 | 小 |
| 5 | 文件变更时间线 | 本项目所有被 Agent 改过的文件集中一页，支持批量撤销/按时间回溯 | 中 |
| 6 | 工作区文件树 | 侧栏项目内文件浏览器，点文件 = 插入 @引用让 Agent 读 | 中 |
| 7 | 费用预算提醒 | cc-switch 已有 limit_daily_usd / limit_monthly_usd 字段，接上：超预算弹窗 + 头部角标 | 小 |
| 8 | 开机自启 | Windows 计划任务 / PM2 守护进程，开机即用 | 小 |

## 💎 第二梯队：高价值 · 中难度

| # | 功能 | 说明 | 预估 |
|---|------|------|------|
| 9 | 多模型对比 | 同一问题并排发给 2-3 个模型，表格对比回答质量/耗时/token/费用 | 中 |
| 10 | Agent 接力 | claude 干完自动把产出交给 codex review 的链式工作流（可视化管道） | 中大 |
| 11 | 定时任务 | cron 表达式定时发消息（每天早上的日报、每晚的构建检查） | 中 |
| 12 | Git 集成 | diff 视图接 git log/commit/checkout；项目组显示分支名 + 脏状态 | 中 |
| 13 | MCP 管理 | 读 cc-switch 的 mcp_servers 表，界面启停/查看 MCP 服务器 | 中 |
| 14 | 消息分支 | 从任意历史消息分叉出新会话（试不同方案不丢原线） | 中 |
| 15 | 局域网访问 | 手机/其他电脑访问 + 简单令牌保护 | 小 |
| 16 | Webhook API | 外部系统（CI/脚本）触发会话并取回结果，配合定时任务很强 | 中 |

## 🔧 第三梯队：实用细节（低成本点缀，可批量做）

| # | 功能 | 说明 |
|---|------|------|
| 17 | 流式速率显示 | 输出时实时显示 tokens/s + 已输出计数 |
| 18 | 消息收藏 | 重要回复标 ⭐，侧栏集中查看 |
| 19 | 快捷键补全 | Ctrl+Tab 切会话、J/K 上下跳消息、Esc 关弹窗统一 |
| 20 | 字体大小调节 | 设置页滑杆，老人小孩都舒服 |
| 21 | 用量报表导出 | 统计页导出 CSV（含明细） |
| 22 | 模型性价比榜 | 各模型平均延迟 / 每千 token 费用 / 成功率排行 |
| 23 | 数据一键备份 | 全部会话/设置/SSH 主机打包 zip，恢复导入 |
| 24 | 旧会话自动归档 | N 天未动自动收起（可配置） |
| 25 | 会话内搜索 | 当前会话内查找定位（配合全局搜索） |
| 26 | 引用回复 | 悬停历史消息可"引用"它来提问 |

## 🧪 实验性（兴趣驱动）

| # | 功能 | 说明 |
|---|------|------|
| 27 | 语音输入 | 浏览器 SpeechRecognition，按住说话 |
| 28 | TTS 朗读 | 回复朗读（后台任务完成时听结果） |
| 29 | 提示词模板变量 | `{{file}}` `{{date}}` `{{clipboard}}` 动态填充 |
| 30 | 夜晚版田园风 | 深色 cottagecore 主题（暖棕夜色，护眼） |
| 31 | 内置迷你编辑器 | 小文件改动不用开 IDE（只读预览 + 简单编辑） |
| 32 | 浏览器扩展 | 网页上选中文字/报错直接发给 AgentHub 处理 |
| 33 | 移动端适配 | 手机浏览器可用（响应式布局 + 触控目标 ≥44px） |
| 34 | i18n | 界面中英文切换 |

## 🚨 新补充：稳定性与自动化（本轮新增）

| # | 功能 | 说明 |
|---|------|------|
| 35 | 供应商健康监测 | 定期 ping 各供应商（延迟/可用性），侧栏红绿灯；cc-switch 已有 provider_health 表可直接读 |
| 36 | 自动故障转移 | 供应商 429/超时自动切换到备用供应商续跑（cc-switch 有 failover 队列概念可参考） |
| 37 | 请求重试策略 | 可配置的自动重试（次数/退避），429 限流时不用手动重发 |
| 38 | 上下文压缩提醒 | 上下文 >80% 时建议"压缩历史"动作（发一条 summarize 指令并新开续接） |
| 39 | 断线重连恢复 | WS 断开后重连，直播中的会话自动接回（现在断了要刷新） |
| 40 | 日志查看器 | 界面里直接看 server.log（排障不用开终端） |

## 🖥️ 新补充：工作流增强

| # | 功能 | 说明 |
|---|------|------|
| 41 | 检查点机制 | Agent 动手前对工作区打快照（文件复制），一键整体回滚——比单文件撤销更强 |
| 42 | 批量会话 | 同一指令发给多个项目/主机的会话并行执行（批量重启服务、批量改配置） |
| 43 | 剪贴板助手 | 发送时自动识别剪贴板里的报错堆栈，询问是否附带 |
| 44 | 错误自动分析 | 输出里的报错自动高亮 + 一键"分析这个错误"按钮 |
| 45 | 代码块运行 | bash/python 代码块旁边加 ▶ 按钮，直接在对应环境（本机/WSL/SSH）执行 |
| 46 | 分屏对比 | 两个会话左右并排（对比两个模型的回答/两个分支的日志） |
| 47 | 会话导入导出 | 导出的 .md 可再导入（跨设备迁移、分享给同事） |
| 48 | 目录同步状态 | 远程项目显示本地↔远程的差异提示（rsync dry-run） |

---

## 📊 推荐路线

**第一批（日常体验立刻变好）**：1 命令面板、2 短语库、4 归档、7 预算提醒、17 速率显示、19 快捷键
**第二批（发挥聚合优势）**：9 多模型对比、5 文件时间线、41 检查点、35 供应商健康
**第三批（自动化进阶）**：11 定时任务、16 Webhook、10 Agent 接力、36 故障转移

## 已完成功能存档（截至 2026-09-08）

多 Agent 并行 · 项目分组侧栏 · cc-switch 供应商/模型/余额 · token 统计（全局+会话+上下文仪表）·
SSH/WSL 远程会话 · 底部多标签终端（PowerShell/WSL/SSH）· 流式时间线（思考/工具/文件 diff 撤销/图片/网页/计划）·
搜索 Ctrl+K · 图片粘贴 · 消息排队 · @文件引用 · 代码高亮 · 置顶 · 编辑重发 · 重试 · 导出 Markdown ·
推理强度 · 模型窗口自定义 · 田园核风主题
